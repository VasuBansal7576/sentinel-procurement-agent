{
  "name": "sentinel",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "apps/web"
  ],
  "scripts": {
    "dev:web": "npm run dev --workspace @sentinel/web",
    "build": "npm run build --workspace @sentinel/web",
    "lint": "npm run lint --workspace @sentinel/web",
    "format": "npm run format --workspace @sentinel/web",
    "typecheck": "npm run typecheck --workspace @sentinel/web",
    "test": "npm run test --workspace @sentinel/web"
  },
  "engines": {
    "node": ">=22"
  }
}
